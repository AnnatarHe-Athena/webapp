#import "../fetchGirls.graphql"

mutation addGirlCells($cells: [CellInput!]!) {
  addGirls(cells: $cells) {
    id
    img
    content
    text
    permission
    fromID
    fromURL
    isCollected
  }
}
